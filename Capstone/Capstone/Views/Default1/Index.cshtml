@model IEnumerable<Capstone.Models.appointments_table>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            appointment_num
        </th>
        <th>
            appointment_date
        </th>
        <th>
            appointment_time
        </th>
        <th>
            users_table
        </th>
        <th>
            service_reason
        </th>
        <th>
            approved
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.appointment_num)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.appointment_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.appointment_time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.users_table.username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.service_reason)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.approved)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
